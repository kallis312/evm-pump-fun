Deploy the contract on ethereum or base by

```shell
npx hardhat run scripts/deployPumpFactory.js --network ethereum
```
```shell
npx hardhat run scripts/deployPumpFactory.js --network base
```

Try running some of the following tasks:

```shell
npx hardhat compile
npx hardhat test
```
